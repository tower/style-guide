
/**
 * Module dependencies.
 */

var fs = require('fs');

/**
 * Expose `style`.
 */

module.exports = style;

/**
 * XXX: Maybe this goes through the
 *      tower source code and cleans it up.
 */

function style(name) {

}